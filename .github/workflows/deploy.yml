# Pipeline CI/CD — sera complété en Semaine 1 Jour 6
name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Placeholder
        run: echo "Pipeline will be configured on Day 6"